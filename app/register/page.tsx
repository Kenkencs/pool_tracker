"use client";
import { FormEvent,useState } from "react";
import { supabase } from "../../lib/supabase";
export default function Register(){const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[msg,setMsg]=useState("");const submit=async(e:FormEvent)=>{e.preventDefault();if(!supabase)return;const {data,error}=await supabase.auth.signUp({email,password,options:{emailRedirectTo:`${location.origin}/setup`}});if(error){setMsg(`注册失败：${error.message}`);return}setMsg(data.session?"✓ 注册并登录成功，正在进入数据管理页…":"✓ 注册成功，请到邮箱点击确认链接后再登录。");if(data.session)setTimeout(()=>location.href="/setup",700)};return <main><section className="screen"><p className="eyebrow">首次使用</p><h1>创建账号</h1><p className="helper">创建后由管理员在 Supabase 设置权限。</p><form className="form-card" onSubmit={submit}><label>邮箱<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required placeholder="name@example.com"/></label><label>密码<input type="password" value={password} minLength={6} onChange={e=>setPassword(e.target.value)} required placeholder="至少 6 位"/></label><button className="primary">注册</button>{msg&&<p className="notice">{msg}</p>}</form><p className="helper">已有账号？<a href="/setup">去登录</a></p></section></main>}
